// let videoList = document.querySelectorAll('img#style-scope#yt-img-shadow');
// videoList.forEach((video) => (video.style.opacity = 0.5));

let videoList = document.querySelectorAll(
  '#video-title.ytd-playlist-renderer, #video-title.ytd-video-renderer'
);
